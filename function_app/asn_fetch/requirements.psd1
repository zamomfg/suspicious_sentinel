# No external PowerShell modules are required — the function uses only built-in
# cmdlets and .NET types (System.IO.Compression). Keeping this empty avoids the
# managed-dependency (Az module) install on cold start.
@{
}
