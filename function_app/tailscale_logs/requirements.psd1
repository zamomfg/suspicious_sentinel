# No external PowerShell modules required — only built-in cmdlets and the
# managed-identity REST endpoint are used. Keeping this empty avoids the
# managed-dependency (Az module) install on cold start.
@{
}
